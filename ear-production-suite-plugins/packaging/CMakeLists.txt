include(DeployVST3)

set(DEP_SEARCH_PATHS "${PROJECT_BINARY_DIR}/lib" "${PROJECT_BINARY_DIR}/VST3" "${PROJECT_BINARY_DIR}/lib/*" "${PROJECT_BINARY_DIR}/VST3/*")
install_all_vst3_dependencies("${EPS_PLUGIN_INSTALL_PREFIX}ear-production-suite" "${DEP_SEARCH_PATHS}")

#if(APPLE)
#  set(CPACK_GENERATOR "DragNDrop")
#else(WIN32)
#  set(CPACK_GENERATOR "NSIS" "ZIP")
#endif()
#set(CPACK_PACKAGE_NAME "EAR Production Suite")
#set(CPACK_PACKAGE_VERSION "${CMAKE_PROJECT_VERSION}")
#set(CPACK_PACKAGE_VENDOR "IRT")
#set(CPACK_PACKAGE_INSTALL_DIRECTORY "ear-production-suite")
#set(CPACK_DMG_DISABLE_APPLICATIONS_SYMLINK ON)
#if(CMAKE_CL_64)
#  set(CPACK_NSIS_INSTALL_ROOT "$PROGRAMFILES64\\\\Common Files\\\\VST3")
#  set(CPACK_NSIS_PACKAGE_NAME "${CPACK_PACKAGE_NAME} ${CPACK_PACKAGE_VERSION} (Win64)")
#else()
#  set(CPACK_NSIS_INSTALL_ROOT "$PROGRAMFILES\\\\Common Files\\\\VST3")
#endif()
#set(CPACK_RESOURCE_FILE_LICENSE "${PROJECT_SOURCE_DIR}/LICENSE.TXT")
#include(CPack)
